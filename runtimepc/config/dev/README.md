# Dev configs (not installed on RPi5)

| File | Use |
|------|-----|
| `stand.pc.yaml` | PC lab / TCP ego-contract |
| `windows-test.yaml` | Windows file-ingest smoke tests |

Production board template: `../board.yaml` → `/etc/ego-runtime/config.yaml`.
